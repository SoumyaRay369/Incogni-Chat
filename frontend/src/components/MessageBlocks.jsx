export const MessageBlocks = ({ message }) => {
    return (
        <div className="w-screen bg-green-700 font-mono font-semibold rounded-md px-1 py-1 text-white text-right mb-2">
            {message}
        </div>
    );
};
